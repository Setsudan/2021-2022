from .mapgame import bigmap


def island(name_player):
    # ? 1. Description du lieu
    # ? 2. Initialisation de la map
    # ? 2.5 Loot + Combat
    # ? 3. Boss
    # ? 4. Envoie sur la prochaine map et on recommence
    desc = "Description de l'ile"
    print(desc)
    bigmap(name_player)
    # boss()
    # islandlvl2() (faut qu'on trouve un moyen d'automatiser ce truc)
